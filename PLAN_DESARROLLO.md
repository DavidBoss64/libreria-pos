# PLAN DE DESARROLLO Y LÓGICA DE NEGOCIO (SISTEMA LIBRERÍA HÍBRIDO)

Este documento define la hoja de ruta del proyecto, dividida en Fases (Sprints). Cada fase debe completarse y probarse antes de iniciar la siguiente.

**Cómo usar este archivo con Claude Code:** los checkboxes (`- [ ]` / `- [x]`) son la única fuente de verdad de qué está hecho. Al terminar una sesión de trabajo, pide explícitamente que se marquen aquí los pasos completados. Al iniciar una sesión nueva, pide que se revise este archivo antes de escribir código, para retomar exactamente donde quedó.

---

## FASE 1: Cimientos, Jerarquía y Autenticación
**Objetivo:** Levantar la base del sistema, configurar PostgreSQL y establecer quién usa el sistema y desde dónde.

- [x] **Paso 1.1: Setup Base:** Instalación de Laravel 13, configuración de la base de datos y despliegue del panel Filament v5.
- [x] **Paso 1.2: Migraciones y Modelos Base:** `sucursales`, `almacenes` y `users`.
- [x] **Paso 1.3: Lógica de Negocio (Jerarquía):**
    - Un usuario (`User`) siempre pertenece a una `Sucursal` (excepto el Super Admin).
    - Una `Sucursal` tiene al menos un `Almacen` (puede ser 'tienda' para ventas o 'deposito' para guardado).
- [x] **Paso 1.4: Interfaz — ARQUITECTURA MULTI-PANEL:**
    - El sistema usa **tres paneles Filament independientes**: `admin` (Administrador/Gerencia), `pos` (Vendedor + Cajero) y `almacen` (Almacenero). Ver reglas detalladas en `CLAUDE.md`.
    - [x] Panel base en `/admin` (instalación por defecto de Filament) con los Recursos de Sucursales, Almacenes y Usuarios. Este panel se conserva tal cual como el futuro panel `admin`.
    - [x] Crear panel `pos` (`php artisan make:filament-panel pos`).
    - [x] Crear panel `almacen` (`php artisan make:filament-panel almacen`).
    - [x] Implementar `canAccessPanel(Panel $panel)` en el modelo `User` para restringir login por rol.
- [x] **Paso 1.4.1: Autogestión de cuenta (perfil y recuperación de contraseña):**
    - [x] Página de perfil (`->profile()`) habilitada en los 3 paneles (`admin`, `pos`, `almacen`), con formulario propio (`App\Filament\Auth\EditProfile`) que usa `nombres`/`apellidos` en vez del campo `name` por defecto de Filament.
    - [x] Recuperación de contraseña (`->passwordReset()`) habilitada en los 3 paneles, usando el broker nativo de Laravel y el mailer configurado en `.env` (`MAIL_MAILER=log` en desarrollo).

> **PENDIENTE ANTES DE PRODUCCIÓN:** `MAIL_MAILER=log` solo escribe el correo de recuperación de contraseña en `storage/logs/laravel.log`, no lo envía realmente. Configurar un mailer real (SMTP, Mailgun, Resend, SES) antes de salir a producción.

---

## FASE 1.5: Seguridad, Roles, Permisos y Aislamiento por Sucursal
**Objetivo:** Blindar el sistema asegurando que cada usuario solo vea y haga lo que le corresponde según su rol operativo, **y** solo los datos de su propia sucursal/almacén — usando exclusivamente herramientas nativas de Laravel/Filament.

> **Decisión de arquitectura:** se usa `Policies` de Laravel basadas directamente en el campo `users.role` (ya existente como `enum`), **no** `spatie/laravel-permission` ni `Filament Shield`. Los 4 roles son fijos y cerrados según `LOGICA_NEGOCIO.md` sección 2 — no se prevén roles nuevos ni permisos configurables por el propietario. Agregar un paquete de permisos dinámicos (con sus tablas `roles`/`permissions`/pivots) sería una fuente de verdad duplicada frente al enum, y complejidad innecesaria para este negocio. Si en el futuro se requieren permisos configurables desde la UI, se reevalúa esta decisión.

- [x] **Paso 1.5.1: Helpers de rol en el modelo `User`:** métodos auxiliares (`isAdmin()`, `isVendedor()`, `isCajero()`, `isAlmacenero()`) para simplificar chequeos repetidos en Policies y en `canAccessPanel()`.
- [x] **Paso 1.5.2: Policies por recurso:** crear `SucursalPolicy`, `AlmacenPolicy` y `UserPolicy` — **regla estricta:** solo `admin` puede `viewAny/create/update/delete` sobre Sucursales, Almacenes y Usuarios. Los demás roles no deben ver estos menús ni acceder por URL directa.
- [x] **Paso 1.5.3: Registro en Filament:** vincular las Policies a los Resources correspondientes en el panel `admin` (Filament las respeta automáticamente si siguen la convención de nombres de Laravel). Incluye los métodos plurales (`deleteAny`/`forceDeleteAny`/`restoreAny`) requeridos por las bulk actions — ver regla 13 de `CLAUDE.md`.
- [x] **Paso 1.5.4: Verificación manual:** iniciar sesión con un usuario de cada rol y confirmar que cada uno ve y puede hacer únicamente lo descrito en `LOGICA_NEGOCIO.md` sección 2 (ej. un Vendedor no debe poder entrar a `/admin` ni ver el Resource de Usuarios).
- [x] **Paso 1.5.5: Aislamiento por sucursal (Trait reutilizable):** crear `app/Filament/Concerns/AislaPorSucursal.php`, un Trait para Resources de Filament que sobreescribe `getEloquentQuery()` (no un Global Scope de Modelo — evita romper Jobs/Commands/Services que corren sin usuario autenticado). Documentado y listo para usarse desde el primer Resource de negocio que se cree en Fase 2 en adelante. No se aplica a ningún Resource todavía porque `Sucursal`, `Almacen` y `User` no lo necesitan (son de gestión exclusiva del `admin`, ya cubierto por Policies).

> **Nota sobre verificación de aislamiento:** no se puede probar el Trait de forma real hasta que exista el primer "Resource de negocio" (uno que un Vendedor/Cajero/Almacenero vea con datos propios de su sucursal, ej. `Inventario`, `Venta`, `Traspaso` en Fase 3/4). Por eso esa verificación NO es un paso de la Fase 1.5 — es una **regla recurrente** (ver `CLAUDE.md` regla 9 actualizada): cada vez que se cree un Resource de negocio en Fases 3+, ese mismo paso debe incluir aplicar el Trait y verificar con dos usuarios de sucursales distintas antes de marcarse como completado. La Fase 1.5 se da por completada con los Pasos 1.5.1 a 1.5.5.

---

## FASE 2: Catálogo de Productos y Precios Dinámicos
**Objetivo:** Estructurar el inventario teórico (lo que se vende, no las cantidades aún) y la lógica de precios.

- [x] **Paso 2.1: Migraciones:** `marcas`, `categorias`, `productos` y `producto_variantes`. Modelos con relaciones y casts. Verificado con migración real en PostgreSQL y test de relaciones/casts.
- [x] **Paso 2.2: Lógica de Negocio (Precios Escalonados):**
    - Un producto general (ej. "Cuaderno 100 hojas") tiene variantes (ej. "Rojo", "Azul").
    - Cada variante maneja un `codigo_interno` (búsqueda rápida) y un `codigo_barras`.
    - **Regla de Precios:** Se definen tres precios (`unidad`, `docena`, `mayor`). El sistema debe ser capaz de consultar el Service correspondiente para saber qué precio aplicar según la cantidad que el cliente lleve.
    - **Precio Mayorista (decisión ya resuelta, ver `LOGICA_NEGOCIO.md` sección 4):** se activa por umbral automático de cantidad (configurable, migrado a `configuracion_negocio` en Paso 4.4) **o** por selección manual del Vendedor/Cajero para clientes recurrentes. Nunca hardcodear el umbral en el Service.
    - **Implementado:** `App\Enums\TipoPrecioAplicado` (unidad/docena/mayor), `App\Services\PrecioService::calcularPrecio(int $productoVarianteId, int $cantidad, bool $forzarMayorista = false): PrecioCalculado` (math decimal exacto vía `bcmul`, valida `cantidad > 0` con `InvalidArgumentException` y umbral > 12 con `RuntimeException`), `App\Services\PrecioCalculado` (DTO de solo lectura). Verificado con 6 casos de prueba (unidad, docena, mayor automático, mayor forzado, cantidad inválida, umbral mal configurado).
- [x] **Paso 2.3: Interfaz:** Formularios en el panel `admin` (catálogo maestro) para crear productos con sus variantes y precios. El panel `pos` solo consulta (read-only) para armar pre-ventas. **Incluir calculadora de margen:** junto a cada campo de precio, mostrar en vivo el margen de ganancia resultante frente a `costo_real` (campo reactivo de Filament, sin columnas nuevas en base de datos), idealmente editable en ambos sentidos (precio → margen, o margen → precio sugerido).
    - **Implementado:** `MarcaResource`/`CategoriaResource` (slug auto-generado + `unique(ignoreRecord: true)`) y `ProductoResource` en el panel `admin`, con `VariantesRelationManager` anidado (calculadora de margen bidireccional que maneja `costo_real` vacío/0 sin dividir por cero). `App\Filament\Pos\Resources\Productos\ProductoResource` de solo lectura (únicamente páginas `index`/`view`, sin `create`/`edit`) en el panel `pos`, con vista mejorada visualmente (imagen + nombre + marca + categoría agrupados, detalle de variantes sin exponer `costo_real`). 4 Policies nuevas (`MarcaPolicy`, `CategoriaPolicy`, `ProductoPolicy`, `ProductoVariantePolicy`, las 10 habilidades cada una — `viewAny`/`view` abiertas a cualquier usuario activo en las 2 últimas). Verificado con tests (admin CRUD, edición de variantes, catálogo read-only del vendedor en `pos`, bloqueo 403 de `/admin` para roles no-admin, y los 6 casos del cálculo de margen incluyendo costo cero/vacío/no numérico).

> **REVISIÓN POST-COMPLETADO (Paso 2.3): imagen de producto por URL externa, no por archivo subido al servidor.** Decisión del propietario: en vez de `FileUpload`, `imagen_principal` se llena pegando una URL de una imagen ya alojada en un servicio externo en la nube (ej. Cloudinary). Campo `TextInput` validado con `->url()`, con vista previa en vivo (`->live()` + elemento que renderiza la imagen mientras se pega el link). No requiere cambio de esquema. Ya no aplica la regla 15 de `CLAUDE.md` (`storage:link`) a este campo específico.

> **DECISIÓN TOMADA (previamente pendiente):** no se implementa `unidad_medida` ni columnas `decimal` en `cantidad` por ahora. Todos los productos (incluida cartulina/forro/contact) se modelan como piezas, rollos o pliegos **enteros**, usando el campo `atributos` (jsonb) para describir color, medida o presentación. **Condición para reabrir esta decisión:** si en el futuro aparece un producto real donde el Vendedor deba vender una cantidad fraccionada a pedido, ahí sí se implementa `unidad_medida` + `decimal`, como una fase dedicada — no antes.

---

## FASE 3: Motor de Inventario y Traspasos (El Kardex) — Completa (3.1-3.6)
**Objetivo:** Controlar el stock real. Esta es la parte más crítica del backend; tolerancia a fallos cero.

- [x] **Paso 3.1: Migraciones:** `inventarios` (con `unique(['almacen_id','producto_variante_id'])` y columna `cantidad_comprometida`), `movimientos_inventario`, `traspasos`, `traspaso_detalles`, `almacen_usuario` (pivote de asignación Almacenero↔Almacén, con `unique(['usuario_id','almacen_id'])` — ver `DATABASE.md` sección 1 y `LOGICA_NEGOCIO.md` sección 2).
- [x] **Paso 3.2: Lógica de Negocio (Kardex Estricto):**
    - **Regla de Oro:** NUNCA se actualiza la tabla `inventarios` directamente.
    - `InventoryAction`/`RegistrarMovimientoInventarioAction`: recibe producto, almacén, cantidad y motivo; usa `lockForUpdate()` sobre la fila de `inventarios` dentro de `DB::transaction()`; crea el registro inmutable en `movimientos_inventario`.
    - **Traspasos:** flujo manual (`solicitado` -> `preparando` -> `en_transito` -> `completado`). Al completarse, `CompletarTraspasoAction` saca stock de un almacén y lo mete en otro.
    - **Relación `User` ↔ `Almacen`:** `belongsToMany(Almacen::class, 'almacen_usuario')` en el modelo `User`.
- [x] **Paso 3.3: Interfaz:** Tablas en el panel `almacen` para ver el stock actual y gestionar/aprobar traspasos entrantes; en el panel `pos`, solo consulta de stock + creación de solicitudes de traspaso por el Vendedor. **Implementado:** `AislaPorAlmacen` (nuevo Trait) + `InventarioResource`/`TraspasoResource` en el panel `almacen` con transiciones de estado y `CompletarTraspasoAction`; `InventarioResource`/`TraspasoResource` en el panel `pos` con `AislaPorSucursal`, formulario de solicitud (Repeater) y `SolicitarTraspasoAction`; `InventarioPolicy`/`TraspasoPolicy` nuevas.

> **RESUELTO (Paso 3.3 cerrado del todo):** el selector de "almacén destino" en `App\Filament\Pos\Resources\Traspasos\Schemas\TraspasoForm` tenía dos huecos: (a) no mostraba a qué sucursal pertenecía cada almacén, y (b) no exigía `tipo = 'tienda'` (un Vendedor podía elegir un `deposito` de su propia sucursal como destino). Corregido: `->options()` ahora filtra por `tipo = AlmacenTipo::Tienda` + `sucursal_id`, con label `"{$almacen->sucursal->nombre} — {$almacen->nombre}"`. Verificado con test dedicado. Suite completa: 28/28.

- [x] **Paso 3.4: Ajuste Manual de Inventario:** interfaz en el panel `admin` que invoca el `InventoryAction` con `tipo_movimiento = 'ajuste'` — única vía real para dar stock inicial antes de que exista Compras (Fase 5). Ver `LOGICA_NEGOCIO.md` sección 5. **Nota:** "Almacenero con previa autorización" (mencionado en `LOGICA_NEGOCIO.md`) queda fuera de alcance — solo `admin` puede ajustar por ahora.
    - **Implementado:** `App\Filament\Pages\AjusteInventario` — Page simple (no Resource, los ajustes son inmutables), solo en panel `admin`, con `canAccess()` explícito (`Auth::user()?->isAdmin()`). Un header `Action` ("Registrar ajuste") con modal: `almacen_id` (Select con label "Sucursal — Almacén (Tipo)"), `producto_variante_id` (Select buscable), `cantidad` (`integer()` + `not_in:0`), `motivo` (Textarea `required`, `maxLength(100)`). Invoca `RegistrarMovimientoInventarioAction::handle()`; si lanza `StockInsuficienteException`, se captura, se notifica y `$action->halt()` sin perder los datos del modal. 6 tests nuevos (403 no-admin, 200 admin, ajuste positivo/negativo, motivo obligatorio, cantidad no puede ser cero). **Suite completa: 34/34.**

- [x] **Paso 3.5: Mejoras de usabilidad de Almacenes/Sucursales + visión panorámica del admin (detectado al probar Fase 3 con datos reales):**
    - **Auto-creación del almacén `tienda` al crear una Sucursal:** `SucursalObserver::created()` crea automáticamente un `Almacen` con `tipo = 'tienda'`, `sucursal_id` = la nueva sucursal, `nombre` = mismo nombre de la sucursal (o "Tienda " . nombre). Elimina el error real detectado hoy (crear el almacén de una sucursal nueva con el `tipo` equivocado, lo que rompe el selector de destino de traspasos sin ningún mensaje de error claro). **Hallazgo durante la implementación:** ya existía un Repeater manual obligatorio de almacenes en `SucursalForm` + `CrearSucursalAction` — se reemplazan por el Observer (única vía), no se mantienen ambos mecanismos en paralelo. La página `CreateSucursal` envuelve la creación completa en `DB::transaction()` (override de `handleRecordCreation()`) para que la creación de la Sucursal y su Almacén sean atómicas — si el Observer falla, no queda una Sucursal huérfana sin almacén. Ver excepción documentada en `CLAUDE.md` regla 2 (Observers para efectos secundarios simples, no Actions). El admin puede seguir agregando almacenes adicionales a mano después, vía el `AlmacenResource` ya existente.
    - **Texto de ayuda en el campo `tipo` del formulario de `Almacen`:** `->helperText()` explicando la diferencia ("Tienda: punto de venta, aquí venden tus vendedores. Depósito: bodega central, no se vende directo aquí.").
    - **Texto de ayuda en la asignación de almacenes a un Almacenero** (`UserResource`, multi-select de `almacenes`): `->helperText()` indicando que normalmente se asignan almacenes de tipo Depósito, no Tienda.
    - **Vista de solo lectura de Inventario y Movimientos en el panel `admin`** (resuelve el pendiente anotado en Fase 3): dos Resources nuevos, `admin`-only, sin transiciones de estado — `InventarioResource` (stock actual, todos los almacenes) y `MovimientoInventarioResource` (Kardex completo: ingresos, salidas, ajustes, con filtros por almacén/producto/tipo/fecha/usuario). Cumple con `LOGICA_NEGOCIO.md` sección 2 ("visión panorámica de todos los almacenes y sucursales") y resuelve la confusión de hoy de "hice un ajuste y no lo vuelvo a ver en ningún lado" — el ajuste sigue siendo inmutable (no editable/eliminable), pero ahora sí consultable.

> **Decisión (Almacenero y ajustes manuales):** por ahora los ajustes manuales de inventario se mantienen exclusivos para `admin` (ver Paso 3.4). Se reevalúa si en la práctica resulta operativamente incómodo que el Almacenero dependa del admin para corregir un conteo físico.

> **Mejora pedida por el propietario (sesión 2026-08-05): ingreso de stock "por caja" en el Ajuste Manual.** El admin recibe mercadería empacada en cajas (ej. "5 cajas de 24 unidades") y tenía que calcular el total de unidades a mano antes de escribirlo en el ajuste. **Principio preservado:** el Kardex (`inventarios`/`movimientos_inventario`) sigue guardando exclusivamente en unidades — "caja" es solo un atajo de captura en la UI, nunca una unidad de stock nueva (misma decisión cerrada de Fase 2 sobre `unidad_medida`).
>   - **Columna nueva `producto_variantes.unidades_por_caja`** (unsignedInteger, nullable — no todo producto viene en caja). Va como columna real, no dentro de `atributos` (jsonb), porque `atributos` es explícitamente "descriptivo únicamente, no afecta cálculos" (`DATABASE.md`) y esto sí afecta el cálculo de cantidad. Campo opcional agregado al formulario/tabla de `VariantesRelationManager` (catálogo, Fase 2).
>   - **`AccionAjusteInventario`** (factory compartido por la Page `AjusteInventario` y `VerStockAlmacen`): `Radio modo_cantidad` ("Por unidad" / "Por caja", default unidad). En modo caja se piden `unidades_por_caja` (precargado desde el producto vía `afterStateUpdated` al elegir la variante, pero editable) y `cantidad_cajas` (+/-, para poder retirar cajas dañadas también), con preview en vivo del total en unidades. Al registrar, si `unidades_por_caja` escrito difiere del guardado en el producto, se actualiza — **decisión confirmada con el propietario:** el proveedor a veces cambia el tamaño de la caja, así que corregirlo desde el mismo modal de ajuste evita un viaje aparte al catálogo. Esa actualización del producto y el movimiento de Kardex quedan en la **misma `DB::transaction()`** (si el ajuste se rechaza por stock insuficiente, el tamaño de caja tampoco cambia).
>   - **Tests:** 4 tests nuevos en `AjusteInventarioPageTest` — precarga reactiva de `unidades_por_caja` al elegir variante (verificado con `Livewire::test()->set()` sobre el flujo real, no `callAction(data:)` directo, que hubiera saltado el `afterStateUpdated`), ajuste por caja registra el total correcto en unidades, ajuste por caja con tamaño distinto actualiza el producto, y que un ajuste por caja rechazado por stock insuficiente no deja el tamaño de caja modificado a medias. **Suite completa: 116/116, sin regresiones. Grupo `concurrency`: 2/2.**
>   - **Alcance deliberadamente NO extendido a Traspasos/Ventas:** ahí el stock ya está fraccionado en unidades individuales dentro del sistema — no hay necesidad real de "traspasar" o "vender" por caja. Este mismo campo queda listo para reutilizarse en Fase 5 (Compras a Proveedores), que es el otro punto de entrada real de mercadería en cajas.

> **Implementado (Paso 3.5):** `App\Observers\SucursalObserver` (registrado en `AppServiceProvider::boot()`) reemplazó el Repeater manual obligatorio de `SucursalForm` — `CreateSucursal` ahora envuelve la creación en `DB::transaction()` para atomicidad. `CrearSucursalAction` eliminado (sin referencias restantes). `AlmacenForm`/`UserForm` con `helperText()` nuevos (el segundo agrega además el `Select multiple` de almacenes para el Almacenero, que la relación ya soportaba pero no tenía UI). `InventarioResource`/`MovimientoInventarioResource` nuevos (`admin`-only, solo lectura, con filtros) + `MovimientoInventarioPolicy`. **Suite completa: 34/34, sin regresiones.** Verificación manual en navegador pendiente de confirmar por el propietario.

- [x] **Paso 3.6: Mejoras operativas de picking y consistencia de detalle de producto (detectado al usar el sistema en la práctica):**
    - **Indicador de stock bajo:** en `InventarioResource` (panel `admin`, y su vista análoga en `almacen`), resaltar visualmente (badge + fila completa vía `recordClasses()`) las filas donde `cantidad <= stock_minimo` (punto de reorden, se alerta al llegarlo o cruzarlo, no solo estrictamente por debajo). No requiere columna nueva — `stock_minimo` ya existe desde Fase 3. Adelanta parte del valor de la alerta que formalmente llega en Fase 6 (Dashboard), sin construir el Dashboard completo todavía.
    - **Detalle enriquecido de producto, consistente en todo el sistema:** la vista de traspaso del Almacenero (panel `almacen`) hoy solo muestra nombre + cantidad por línea — se amplía para mostrar también marca, categoría e imagen, reutilizando el mismo patrón visual ya construido para el catálogo del panel `pos` en Fase 2 (sin exponer `costo_real`, esa regla se mantiene). Aplicar el mismo criterio de consistencia en el formulario de solicitud de traspaso del Vendedor (panel `pos`), que hoy también muestra información mínima.
    - **Cumplimiento parcial en la preparación de traspasos:** migración de `traspaso_detalles.cantidad_preparada` (integer, nullable). Editable solo mientras el traspaso está en estado `preparando`. Validación: no permitir transición a `en_transito` si alguna línea tiene `cantidad_preparada` nulo. **Retrofit obligatorio de `CompletarTraspasoAction`** (Fase 3.2): debe mover stock según `cantidad_preparada`, con fallback a `cantidad` para traspasos existentes sin este campo poblado. El Vendedor ve ambos valores (solicitado vs. preparado) en su propia vista del traspaso, sin pasos adicionales.

> **Implementado (Paso 3.6):**
> 1. **Stock bajo:** `recordClasses()` agregado en ambas `InventariosTable` (`admin` y `almacen`) para resaltar la fila completa (`[&>td]:bg-danger-50`, variante dark incluida), además del badge de color ya existente desde Paso 3.5.
> 2. **Selección en cascada Producto → Variante (panel `pos`, `TraspasoForm`):** el `Select` plano de variantes (sin marca/categoría/atributos — fácil de confundir "Rojo" con "Azul" del mismo producto) se reemplazó por un `Select producto_id` transitorio (`dehydrated(false)`) con preview reactivo (imagen + marca + categoría, mismo patrón que `imagen_preview` de `ProductoForm.php` de Fase 2) + un `Select producto_variante_id` filtrado por el producto elegido, con label armado desde `atributos` (color/medida) + `codigo_interno`, y un helper text con el stock disponible en el almacén origen (lee `Inventario` vía `Get('../../almacen_origen_id')`). `TraspasoInfolist` (pos y almacen) se enriqueció igual: imagen, marca, categoría y atributos de la variante por línea, además de `cantidad` (renombrada "Solicitado") y `cantidad_preparada` ("Preparado"). Nunca se expone `costo_real`.
> 3. **`cantidad_preparada`:** migración aditiva aplicada en Postgres real. Nueva `Action` dedicada `App\Actions\Traspasos\TransicionarTraspasoAEnTransitoAction` (reemplaza el `update()` directo del botón "Despachar") que bloquea la transición si alguna línea tiene `cantidad_preparada` nulo (`PreparacionIncompletaException` nueva). Nueva `Action` de tabla "Registrar preparación" en `almacen` (modal con `Repeater` no-reordenable/no-agregable, `cantidad_preparada` acotado a `[0, cantidad solicitada]`) para que el Almacenero registre lo reunido por línea mientras el traspaso está `preparando`. `CompletarTraspasoAction` retrofit: mueve `cantidad_preparada ?? cantidad`, y **salta la línea sin generar movimiento** si la cantidad a mover es `0` (evita el `InvalidArgumentException` de `RegistrarMovimientoInventarioAction`, que exige cantidad > 0 para ingreso/salida).
> 4. **Tests nuevos:** 2 en `CompletarTraspasoActionTest` (mueve stock según `cantidad_preparada` cuando difiere de lo solicitado; `cantidad_preparada = 0` no genera movimiento ni crea inventario en destino) + 3 en `TransicionarTraspasoAEnTransitoActionTest` nuevo (bloquea si falta preparación, permite con `0` incluido, rechaza traspaso que no está en `preparando`). Se ajustaron 2 tests existentes de `InventarioTraspasoAislamientoUiTest` para incluir el nuevo campo transitorio `producto_id` en el `fillForm`. **Suite completa: 39/39, sin regresiones.**
> 5. **Mejora post-cierre + fix crítico en "Registrar preparación" (almacen):** se enriqueció el modal con vista previa de producto (imagen/marca/categoría/atributos, mismo patrón que el punto 2) y stock disponible en el almacén origen por línea (`hint()`/`hintColor()` en el campo `cantidad_preparada`, con `maxValue` acotado a `min(solicitado, disponible)` para no poder prometer más de lo que físicamente existe). Al implementarlo se introdujo un bug real: un `TextEntry` cuyo `->state()` leía `Get()` de su **propio** nombre de campo (`producto_preview`) causaba recursión infinita y agotamiento silencioso de memoria de PHP (el botón no hacía nada, sin ninguna traza en `storage/logs/laravel.log`). Corregido separando el dato precomputado (`Hidden::make('producto_html')`) del componente que lo muestra. Test de regresión nuevo: `RegistrarPreparacionTraspasoUiTest` (monta la acción vía Livewire y confirma que no lanza excepción/no cuelga). **Suite completa: 40/40.**

- [x] **Paso 3.7: Papelera y restricciones de integridad para entidades maestras (detectado al revisar qué pasa realmente al "eliminar" un Producto):**
    - **Diagnóstico:** no había ningún bug — `estado` (toggle operativo de negocio) y `deleted_at` (papelera, vía `SoftDeletes`) son columnas independientes por diseño; la papelera ya existía de fábrica (Filament la scaffoldea sola cuando el modelo usa `SoftDeletes`): `TrashedFilter` en la tabla + `DeleteAction`/`RestoreAction`/`ForceDeleteAction` individuales en la página Edit (Producto, Marca, Categoria, Almacen, Sucursal) + sus equivalentes `*BulkAction` en la tabla. Los 5 Resources ya sobreescriben `getRecordRouteBindingEloquentQuery()` sin el `SoftDeletingScope`, así que la página Edit de un registro en la papelera carga correctamente (necesario para que `RestoreAction` sea alcanzable).
    - **Gaps reales encontrados y corregidos:**
        1. `ForceDeleteAction` individual (a diferencia de `ForceDeleteBulkAction`, que ya captura excepciones de fábrica) no atrapaba la `QueryException` de Postgres al chocar con un `restrictOnDelete()` (ya existente en el esquema desde Fase 2/3 para producto_variantes/productos/categorias/almacenes/sucursales) — reventaba como página de error cruda en vez de notificación clara.
        2. Sin cascada de papelera Producto → sus ProductoVariante: al eliminar un Producto, sus variantes quedaban huérfanas y "activas" en cualquier vista (traspasos, inventario). Se decidió cascada solo en el sentido de eliminar, no de restaurar (el admin restaura variantes puntuales vía `VariantesRelationManager`, que ya tenía control fila por fila).
        3. Sin regla de negocio que impidiera enviar a la papelera una Marca/Categoría con productos activos (rompe silenciosamente `producto.marca`/`producto.categoria` en cualquier vista, por el Global Scope de `SoftDeletes` en la relación) ni un Almacén/Sucursal con stock físico real (`cantidad > 0`).
    - **Implementado:** `App\Filament\Support\AccionesPapelera` (helper reutilizable, 3 factories: `forceDeleteSeguro()`, `delete(Closure $bloqueadoSi, string $motivo)`, `deleteBulk(...)`) usado en los 5 Resources + `VariantesRelationManager`. Métodos nuevos en modelos: `Marca::tieneProductosActivos()`, `Categoria::tieneProductosActivos()`, `Almacen::tieneStockFisico()`, `Sucursal::tieneStockFisico()`. `App\Observers\ProductoObserver::deleted()` (cascada a variantes) y `SucursalObserver::deleted()` (cascada a almacenes, solo alcanzable si ya no tienen stock) nuevos, registrados en `AppServiceProvider`. 9 tests nuevos (`PapeleraYRestriccionesTest`): lógica de negocio de los 4 métodos de modelo, las 2 cascadas, bloqueo/permiso de envío a papelera de una Marca vía Livewire, y `forceDelete` de un Producto con variantes sin reventar. **Suite completa: 49/49, sin regresiones.**
    - **Revisión de flujo de información general (pedida junto con este paso):** Policies (10/10 métodos en las 10 existentes), `AislaPorSucursal`/`AislaPorAlmacen` aplicados en todos los Resources de negocio que corresponden, y los 5 Resources con papelera ya listos para restaurar. Dos gaps menores encontrados y **corregidos de inmediato (mismo paso, por ser cambios chicos):** (a) `Pos\Resources\Productos\ProductoResource::getEloquentQuery()` ahora filtra `estado = true` — el Vendedor ya no ve productos descontinuados en el catálogo; (b) los Selects de `almacen_origen_id`/`almacen_destino_id` en `TraspasoForm` (pos) ahora también filtran `estado = true`, ya no se puede elegir un almacén desactivado como origen/destino. 2 tests nuevos (uno en `CatalogoUiSmokeTest`, uno en `InventarioTraspasoAislamientoUiTest`). **Suite completa: 51/51, sin regresiones.**

> **Principio general (aplica también a Fase 5 — Compras):** cuando una operación de negocio con varias líneas (traspaso, compra) puede completarse con menos cantidad de la solicitada por línea, se registra la cantidad realmente ejecutada en una columna separada (`cantidad_preparada`, y a futuro `cantidad_recibida` en compras) — nunca se asume que la cantidad ejecutada siempre coincide con la solicitada. La Action de cierre opera sobre la cantidad ejecutada.

> **DECISIÓN TOMADA (previamente nota abierta):**
> 1. **Reserva de stock en Pre-venta:** se descartó la reserva dura. En su lugar: (a) `inventarios.cantidad_comprometida` como indicador visual no bloqueante; (b) validación real con `lockForUpdate()` en el cierre de venta (Fase 4); (c) expiración automática de pre-ventas abandonadas (`ventas.expira_en`).
> 2. **Restricción única en `inventarios`:** resuelto — `unique(['almacen_id', 'producto_variante_id'])`.
> 3. **Modelo "hub and spoke" (Almacenero ↔ Almacenes):** vía tabla pivote `almacen_usuario`, no `sucursal_id`.
> 4. **Ciclo de traspaso conducido enteramente por el Almacenero** (sin confirmación de recepción separada en la sucursal destino) — ver `LOGICA_NEGOCIO.md` sección 5 para el detalle y la condición de reapertura.

- [x] **Paso 3.8: Widget de contexto del usuario en el Dashboard (panels `pos` y `almacen`):** el Dashboard por defecto de Filament se veía vacío (solo saludo + info de versión de Filament, ruido técnico sin valor para el negocio). Se agrega un widget simple y visible que confirma de un vistazo en qué sucursal/almacén está operando el usuario — reduce errores por confusión de contexto (ej. hacer un traspaso pensando que se está en otra sucursal).
    - **Implementado:** `App\Filament\Pos\Widgets\MiContextoWidget` (Stat: nombre de la sucursal del usuario + su rol como descripción; "Sin sucursal asignada" en rojo si no tiene) y `App\Filament\Almacen\Widgets\MiContextoWidget` (Stat: nombre(s) del/los almacén(es) asignados vía `almacen_usuario`; "Sin almacenes asignados" en rojo si no tiene ninguno). Ambos son `StatsOverviewWidget` de Filament (simple, sin gráficos). Se quitó `FilamentInfoWidget` de ambos paneles (`PosPanelProvider`/`AlmacenPanelProvider`) por ser info de versión sin valor para un usuario real; los widgets nuevos se autodescubren vía el `discoverWidgets()` ya existente. 4 tests nuevos (`MiContextoWidgetTest`): sucursal/rol visibles, aviso si falta sucursal, almacenes visibles, aviso si no hay almacenes asignados. **Suite completa: 55/55, sin regresiones.**

- [x] **Paso 3.9: Selector de producto enriquecido en el ajuste + navegación de stock por almacén (detectado al usar el ajuste manual del Paso 3.4 con catálogo real):** el `Select` de producto del modal de ajuste solo mostraba `"{producto} ({codigo_interno})"`, sin distinguir variantes del mismo producto por sus `atributos` (color/medida/etc.), y el admin elegía almacén y producto "a ciegas" sin ver qué stock existía realmente ahí — la vista de solo lectura ya existía (`InventarioResource`, Paso 3.5) pero desconectada del ajuste.
    - **Implementado:** se extrajo la cascada Producto → Variante ya construida para `TraspasoForm` (Paso 3.6) a `App\Filament\Support\SelectorProductoVariante` (label desde `atributos`, preview con imagen/marca/categoría), reutilizada ahora también en el ajuste — `TraspasoForm` no cambia de comportamiento, solo delega. El modal de ajuste se extrajo a un factory compartido `App\Filament\Support\AccionAjusteInventario::make(?Almacen $almacenFijo = null)`, usado tanto por `AjusteInventario` (Page del menú, sin almacén fijo, tal como antes) como por la nueva página `App\Filament\Resources\Almacens\Pages\VerStockAlmacen` (ruta `/admin/almacenes/{record}/stock`, registrada en `AlmacenResource`): tabla de solo lectura del inventario de ese almacén (imagen, marca, categoría, atributos de variante, resaltado de stock bajo) con el botón "Registrar ajuste" ya trayendo el almacén fijado (`Select` deshabilitado + `default()` + `dehydrated()`) y un `helperText` con el stock actual de la variante elegida en ese almacén. Se entra desde una acción de fila "Ver stock" nueva en `AlmacensTable`, sin agregar entrada de menú. 5 tests nuevos (`VerStockAlmacenPageTest`: 403 no-admin, 200 admin, aislamiento de la tabla por almacén, ajuste registrado con el almacén ya fijado) + los 4 tests de `AjusteInventarioPageTest` que llaman a la acción se ajustaron para incluir el campo transitorio `producto_id` del nuevo selector. **Suite completa: 59/59, sin regresiones** (incluye traspasos, que siguen usando el mismo `SelectorProductoVariante` sin cambios de comportamiento).

- [x] **Paso 3.10: Visibilidad de stock de depósitos para el Vendedor + resaltado de fila roja consistente (detectado en revisión de salud del sistema, sesión 2026-08-01):**
    - **Fila roja incompleta:** `InventariosTable` del panel `pos` no tenía `recordClasses()` (solo el número en badge rojo), a diferencia de `admin`/`almacen` (Paso 3.6). Corregido: mismo patrón de resaltado de fila completa aplicado en los 3 paneles.
    - **Vendedor sin visibilidad de stock de depósitos:** el Vendedor podía solicitar traspaso desde cualquier almacén tipo `deposito` (`TraspasoForm`), pero no tenía forma de explorar su stock de antemano para decidir de cuál pedir — solo veía cantidad producto por producto, ya dentro del formulario. **Decisión (confirmada con el propietario):** se amplía la base de visibilidad de `Pos\InventarioResource` (antes solo almacenes de la propia sucursal) para incluir también todos los almacenes tipo `deposito` activos, sin importar su sucursal dueña — son los únicos orígenes válidos de traspaso hoy. **Deliberadamente NO se incluye el stock de tiendas de otras sucursales**: no existe un flujo de traspaso tienda-a-tienda que use ese dato, y mostrarlo rompería el aislamiento por sucursal de `LOGICA_NEGOCIO.md` sección 1 sin ningún propósito operativo — se reabre esta decisión solo si el negocio pide explícitamente ese flujo.
    - **Implementado:** `InventarioResource::scopeQueryToSucursal()` (panel `pos`) ampliado a `sucursal_id = propia OR (tipo = deposito AND estado = true)`. `ListInventarios` (panel `pos`) con dos pestañas (`Filament\Schemas\Components\Tabs\Tab` vía `getTabs()`): "Mi sucursal" (por defecto, comportamiento sin cambios) y "Almacenes (traspaso)" (solo depósitos). Columna "Sucursal" nueva (toggleable) en `InventariosTable` del panel `pos` para distinguir depósitos de distintas sucursales dueñas. **Suite completa: 59/59, sin regresiones** (incluye `InventarioTraspasoAislamientoUiTest::test_vendedor_solo_ve_stock_de_su_propia_sucursal`, que verifica el comportamiento por defecto de la pestaña "Mi sucursal").

- [x] **Paso 3.11: Fix — la papelera rompía la vista de traspasos históricos (bug real detectado en producción manual, misma sesión que el Paso 3.10):** al ver un traspaso ya completado (`/pos/traspasos/{id}` o su equivalente en `almacen`) cuya variante de producto había sido enviada a la papelera *después* de crearse el traspaso, la página tronaba con `Attempt to read property "producto" on null` — porque `belongsTo()` excluye por defecto los registros en papelera, y `TraspasoInfolist` asumía que la relación siempre resolvía. Contradice el principio ya establecido en el Paso 3.7 (la papelera no debe corromper vistas históricas).
    - **Implementado:** `->withTrashed()` agregado en la cadena completa de relaciones usada por la vista histórica: `TraspasoDetalle::productoVariante()`, `ProductoVariante::producto()`, `Producto::marca()`, `Producto::categoria()`. Corrige el bug en ambos paneles (`pos` y `almacen`, que comparten el mismo `TraspasoInfolist` roto) sin tocar el Infolist directamente — el fix vive en el modelo, así que cubre también cualquier vista futura que recorra esta misma cadena. **Suite completa: 59/59, sin regresiones.** Verificado manualmente con el traspaso real que disparó el error.
    - **Nota para Fase 4/5:** el mismo patrón (documento histórico con FK a una entidad de catálogo que luego se manda a papelera) se repetirá con `venta_detalles.productoVariante` (Fase 4) y `compra_detalles.productoVariante` (Fase 5) — aplicar `withTrashed()` en esas relaciones desde que se creen, no esperar a que se reporte el mismo bug de nuevo.

> **Nota técnica registrada (sesión 2026-08-03, sin paso de plan asociado): tema visual custom de Filament vía Vite.** Se encontraron, sin commit y sin documentar previamente, `->viteTheme('resources/css/filament/theme.css')` agregado en los 3 `PanelProvider` (`admin`, `pos`, `almacen`) + el archivo `resources/css/filament/theme.css` nuevo (que solo importa el theme base de Filament y declara los `@source` de `app/Filament` y `resources/views/filament` — sin overrides de color/tipografía todavía) + `vite.config.js` actualizado para incluir ese entrypoint. Es la vía estándar de Filament v5 para personalizar el CSS del panel (íconos, colores custom, fuentes) más allá de `->colors()`. No cambia ningún comportamiento hoy (el archivo no tiene overrides aún), así que no hay drift funcional que corregir — queda documentado para que una sesión futura sepa que ese archivo existe y es el lugar correcto para futuras personalizaciones visuales, en vez de crear un segundo mecanismo en paralelo.

- [x] **Paso 3.12: Detalle enriquecido de stock (panel `pos`) + atajos hacia "Nueva solicitud de traspaso" (pedido en sesión 2026-08-03):** la tabla de `Pos\InventarioResource` solo mostraba nombre + `codigo_interno` de la variante, sin marca/categoría/atributos (color, medida) — insuficiente para distinguir variantes del mismo producto de un vistazo. Además, pedir un traspaso obligaba a volver a buscar el producto desde cero en el formulario, aunque el Vendedor ya lo hubiera encontrado en la pantalla de stock.
    - **Detalle enriquecido:** `Pos\Resources\Inventarios\Tables\InventariosTable` ahora incluye imagen, marca y categoría (toggleables) y usa `SelectorProductoVariante::labelVariante()` para la descripción de cada fila (mismo helper ya usado en `VerStockAlmacen` y `TraspasoInfolist`, no un criterio nuevo) — con `->modifyQueryUsing()` para evitar N+1 al cargar `almacen.sucursal` y `productoVariante.producto.marca/categoria`.
    - **Atajo por fila:** nueva `Action` "Solicitar", visible en **ambas** pestañas de `Pos\InventarioResource` ("Mi sucursal" y "Almacenes (traspaso)"), que navega a `Pos\Resources\Traspasos\Pages\CreateTraspaso` con `producto_variante_id` en la query string. Si la fila es de un almacén tipo `deposito` (pestaña "Almacenes"), también precarga `almacen_origen_id` — si es de la tienda propia (pestaña "Mi sucursal"), ese parámetro se omite y el Vendedor elige el depósito de origen en el formulario, igual que haría manualmente.
    - **Atajo por selección múltiple:** `BulkAction` "Solicitar traspaso de los seleccionados", con el mismo criterio — **nunca bloquea la selección**. Redirige siempre con `variantes` (CSV de IDs); precarga `almacen_origen_id` únicamente cuando la selección completa proviene de un único almacén depósito (ambiguo con dos depósitos distintos, o inexistente si se seleccionó desde "Mi sucursal" → se omite y se deja elegir). Usa `redirect()` dentro del `->action()` (Livewire v3 intercepta el `redirect()` global durante una acción).
    - **Corrección post-implementación (mismo día):** la primera versión exigía que la selección fuera 100% de un almacén depósito y bloqueaba con un aviso en caso contrario — esto rompía el caso de uso real pedido por el Vendedor: elegir varios productos en rojo (stock bajo) desde la pestaña "Mi sucursal" y pedir el traspaso directo, sin tener que ir primero a "Almacenes" a buscar los mismos productos ahí. Se corrigió a "precarga cuando es inequívoco, si no deja elegir" en vez de "bloquea si no es inequívoco".
    - **`TraspasoForm` (pos):** el `Select` de almacén origen y el `Repeater` de líneas ahora tienen `->default()` que leen esa query string (`itemsIniciales()` resuelve `producto_variante_id` o `variantes` a filas ya armadas con `cantidad = 1`); el `Select` de almacén destino se auto-completa con la tienda de la propia sucursal del Vendedor (conveniencia general, no depende de query string). Sin query string ni cambio de comportamiento: sigue arrancando con una fila vacía, exactamente igual que antes.
    - **Riesgo de compatibilidad:** ninguno de estos cambios toca `SolicitarTraspasoAction` ni el Kardex — son solo defaults/acciones de UI sobre el mismo formulario y la misma Action de siempre. 9 tests (`PosStockAccionesRapidasTest`): columnas enriquecidas visibles, botón "Solicitar" visible en ambas pestañas, precarga de origen solo en filas/selecciones de depósito, alta completa vía query string sin `fillForm()`, alta manual sigue exigiendo elegir producto, selección de depósitos distintos y selección desde "Mi sucursal" redirigen sin precargar origen, selección de un mismo depósito sí lo precarga. **Suite completa: 68/68, sin regresiones.**

> **Nota técnica registrada (sesión 2026-08-03): `database/seeders/DatosPruebaSeeder.php`.** Seeder de datos de prueba realistas para desarrollar Fase 4 sobre un catálogo/organización ya poblados — no es parte del checklist de fases (no aplica a producción), pero se documenta para que sesiones futuras no lo re-creen ni lo confundan con datos reales. Genera: 2 sucursales (`Sucursal Central`, `Sucursal Norte`, cada una con su almacén `tienda` auto-creado por `SucursalObserver`) + 1 `Depósito Central` adicional; 6 usuarios, uno por rol operativo relevante (`admin@libreria.test`, `vendedor.central@libreria.test`, `cajero.central@libreria.test`, `vendedor.norte@libreria.test`, `cajero.norte@libreria.test`, `almacenero@libreria.test` — asignado al depósito vía `almacen_usuario`), todos con contraseña `password`; 4 marcas, 5 categorías, 20 productos con 27 variantes (algunas con `atributos` de color/medida/tipo para probar la cascada `SelectorProductoVariante`); y el stock inicial de cada variante en los 3 almacenes, registrado con `RegistrarMovimientoInventarioAction` (tipo `ajuste`, igual que el Paso 3.4 real — nunca se escribe `inventarios` directamente). Deliberadamente deja algunas combinaciones en stock bajo o en cero para poder probar el resaltado de stock bajo (Paso 3.6) y el flujo de traspaso sin tener que fabricar esos casos a mano. `DatabaseSeeder::run()` ahora solo llama a este seeder (se quitó el `WithoutModelEvents` del scaffold original de Laravel porque desactivaba `SucursalObserver`, rompiendo la auto-creación del almacén `tienda`). Pensado para `php artisan migrate:fresh --seed`; volver a correrlo sobre datos existentes falla por los `unique` de slug/código.

---

## FASE 4: Módulo de Ventas POS (Pre-venta) — Completa (4.1-4.5)
**Objetivo:** El corazón de la tienda física. Flujo rápido para evitar filas largas.

- [x] **Paso 4.1: Migraciones:** `clientes`, `ventas` (con `expira_en`), `venta_detalles` (con `tipo_precio_aplicado`).
    - **Implementado:** migraciones `clientes` (softDeletes, `email` único nullable), `ventas` (`numero_ticket` nullable+único — se completa recién tras el insert a partir del `id`, ver regla 11 de `CLAUDE.md`; `estado` indexado; FKs a `sucursales`/`users`/`clientes` con `restrictOnDelete()`, mismo criterio que Fase 3) y `venta_detalles` (`venta_id` con `cascadeOnDelete()`, mismo patrón que `traspaso_detalles`). 3 enums nuevos con `HasColor`/`HasLabel` (`VentaEstado`, `VentaOrigen`, `VentaMetodoPago`, mismo patrón que `TraspasoEstado`/`AlmacenTipo`) — reutiliza `TipoPrecioAplicado` (Fase 2) para `venta_detalles.tipo_precio_aplicado`, no se duplica. Modelos `Cliente` (SoftDeletes, password oculto/hasheado), `Venta` y `VentaDetalle::productoVariante()` con `->withTrashed()` desde el inicio (aplica la nota del Paso 3.11: no esperar a que se reporte el mismo bug de nuevo). Sin Actions/Services todavía (llegan en el Paso 4.2). Verificado con migración real en PostgreSQL + smoke test manual vía Tinker (creación, relaciones, casts de enum, generación de `numero_ticket`) dentro de una transacción revertida — sin tests automatizados nuevos porque este paso no incluye ningún `Action` que modifique `inventarios`/saldos (regla 10 de `CLAUDE.md` no aplica todavía). **Suite completa: 68/68, sin regresiones.**
- [x] **Paso 4.2: Lógica de Negocio (Flujo Pre-venta):**
    - **Vendedor:** Atiende al cliente, arma la canasta en el sistema. Se crea una `venta` con estado `pendiente`, `origen = 'pos'` y se le asigna un `cliente_temporal` (ej. "Juan Polera Roja").
    - **Cajero:** Busca "Juan Polera Roja" en su pantalla. Confirma los productos.
    - **Pago:** Se selecciona método (Efectivo, QR, etc.). Si es QR, se guarda el ID del banco en `referencia_pago`.
    - **Cierre:** El sistema ejecuta el `InventoryAction` de la Fase 3, descuenta el stock y pasa la venta a `completado`. No se emite comprobante fiscal, solo el `numero_ticket` interno.
    - **Nota:** se descartó el control de apertura/cierre de caja (`turnos_caja`) para el MVP — ver `LOGICA_NEGOCIO.md` sección 7. Queda como mejora futura opcional.
    - **Decisión tomada con el propietario (sesión 2026-08-03):** duración de `expira_en` = **1 hora**, temporalmente en `config/ventas.php` (`expira_preventa_minutos`, mismo patrón que `umbral_mayor` en `config/precios.php` de Fase 2) hasta que el Paso 4.4 cree `configuracion_negocio` y lo migre ahí junto con `umbral_mayor`. El job programado de cancelación automática se construye en este mismo paso (no se pospone).
    - **Implementado:**
      1. `App\Actions\Inventario\AjustarComprometidoAction` — nuevo, ajusta `inventarios.cantidad_comprometida` (indicador no bloqueante, sin Kardex) con el mismo patrón upsert-no-op + `lockForUpdate()` que `RegistrarMovimientoInventarioAction`.
      2. `App\Actions\Ventas\CrearPreventaAction` (Fase A, Vendedor): crea la `Venta` (`pendiente`, `origen=pos`, `expira_en` = ahora + `config('ventas.expira_preventa_minutos')`), genera `numero_ticket` a partir del `id` recién insertado (regla 11 de `CLAUDE.md`), crea cada `VentaDetalle` usando `PrecioService::calcularPrecio()` (Fase 2, sin duplicar lógica de precios) e incrementa `cantidad_comprometida` en el almacén `tienda` de la sucursal — nunca toca stock real. Exige cliente_id o cliente_temporal.
      3. `App\Actions\Ventas\CancelarPreventaAction`: cancela una pre-venta `pendiente` liberando su `cantidad_comprometida`. Persiste como `estado = anulado` — **nota de consistencia corregida:** `LOGICA_NEGOCIO.md` sección 3 dice "se marcan como `cancelado`", pero el enum real de `ventas.estado` en `DATABASE.md`/la migración del Paso 4.1 solo tiene `anulado` (no existe un valor `cancelado` separado); se usa `anulado` como el mismo concepto, documentado aquí para no reabrir la duda.
      4. `App\Actions\Ventas\CerrarVentaAction` (Fase B, Cajero): valida que el pago digital (transferencia/tarjeta/QR) traiga `referencia_pago` obligatorio; por cada línea usa `RegistrarMovimientoInventarioAction` (Fase 3, con su propio `lockForUpdate()`) para la salida real de stock. Si una línea puntual no tiene stock suficiente (venta concurrente), se **rechaza esa línea únicamente** (se borra el `VentaDetalle`, se libera su `cantidad_comprometida`, se reporta en `ResultadoCierreVenta::$itemsRechazados`) y el resto de la venta se completa igual — regla explícita de `LOGICA_NEGOCIO.md` sección 3B ("no se cancela toda la venta automáticamente"). Si **ninguna** línea tiene stock, lanza `VentaSinStockDisponibleException` y revierte todo (la venta queda `pendiente` intacta). Nuevas DTOs `ItemRechazado`/`ResultadoCierreVenta` (`final readonly`, mismo patrón que `PrecioCalculado` de Fase 2).
      5. `App\Console\Commands\CancelarPreventasVencidasCommand` (`ventas:cancelar-preventas-vencidas`) + registro en `routes/console.php` vía `Schedule::command(...)->everyFiveMinutes()` (Laravel 13, sin `Kernel.php`) — cancela toda `Venta` `pendiente` con `expira_en` ya pasado, reutilizando `CancelarPreventaAction`.
      6. `Sucursal::almacenTienda(): ?Almacen` — helper nuevo, reutilizado por los tres Actions de venta para ubicar el almacén `tienda` de la sucursal (nunca `sucursal_id` directo en `inventarios`, que se indexa por `almacen_id`).
      7. **Concurrencia (regla 17 de `CLAUDE.md`):** `CerrarVentaAction` no implementa su propio `lockForUpdate()` — delega en `RegistrarMovimientoInventarioAction`, cuyo bloqueo de fila real ya está cubierto por `InventarioConcurrenciaTest` (Fase 3, grupo `concurrency`). Decisión explícita: no se duplicó un arnés de dos procesos aparte para este Action porque verificaría el mismo primitivo/la misma fila ya probada — solo se agregó el invariante de negocio secuencial en la suite normal (ítem sin stock se rechaza sin dejar stock negativo).
    - **Verificado con migración/Actions reales en PostgreSQL:** flujo completo Vendedor→Cajero probado vía Tinker sobre datos del seeder (`DatosPruebaSeeder`), dentro de una transacción revertida — pre-venta creada, cerrada, stock y Kardex correctos. 11 tests nuevos (`CrearPreventaActionTest`, `CancelarPreventaActionTest`, `CerrarVentaActionTest` — incluye el rechazo parcial de línea sin stock y el caso "sin ningún ítem disponible" —, `CancelarPreventasVencidasCommandTest`). **Suite completa: 79/79, sin regresiones.**
    - **Nota fuera de alcance (detectada, no relacionada a este paso):** se encontró y corrigió un error de sintaxis sin commitear en `app/Filament/Resources/Almacens/Tables/AlmacensTable.php` (backtick suelto que rompía el autoload de toda la suite) — no se tocó nada más de ese archivo.
- [x] **Paso 4.3: Interfaz:** Pantalla POS optimizada en el panel `pos` para Vendedores (armar pedido) y Cajeros (cobrar). **Recordatorio (CLAUDE.md regla 9):** aplicar `use AislaPorSucursal;` al Resource de `Venta` (columna `sucursal_id` directa) y verificar con dos usuarios de sucursales distintas antes de marcar este paso como completado.
    - **Implementado:** `App\Filament\Pos\Resources\Ventas\VentaResource` (`use AislaPorSucursal;` sin override — `Venta.sucursal_id` es columna directa, a diferencia de Inventario/Traspaso). Un solo Resource compartido por Vendedor y Cajero en el panel `pos`, separado por `VentaPolicy` (10 métodos estándar + una habilidad custom `cobrar()`, ver regla 13 de `CLAUDE.md` — Filament no limita a las 10): `create()`/`update()` (usado para "Cancelar") → Vendedor/admin; `cobrar()` → Cajero/admin; `delete*`/`restore*`/`forceDelete*` → `false` (registro de auditoría, igual que `Traspaso`).
      - **`VentaForm`** (Create, solo Vendedor): sección Cliente (`Select cliente_id` buscable con `createOptionForm`/`createOptionUsing` para registrar un cliente nuevo sin salir del formulario, + `TextInput cliente_temporal`; ambos con `requiredWithout` cruzado — al menos uno obligatorio) y `Repeater detalles` reutilizando la cascada Producto→Variante de `SelectorProductoVariante` (Fase 3), con `Toggle forzar_mayorista` por línea y una vista previa en vivo del precio/subtotal calculado vía `PrecioService::calcularPrecio()` (Fase 2) + un total estimado agregado — todo informativo, el total real siempre lo calcula el servidor en `CrearPreventaAction`.
      - **`CreateVenta`** override de `handleRecordCreation()`: arma `sucursal_id`/`vendedor_id` desde `Auth::user()` (nunca del formulario) y delega en `CrearPreventaAction` (Paso 4.2) — controlador delgado, cero lógica de negocio en la página.
      - **`VentasTable`**: acción "Cobrar" (modal `metodo_pago` + `referencia_pago` condicional, visible solo si `estado=pendiente` y `can('cobrar', $record)`) invoca `CerrarVentaAction`, mostrando en la notificación si hubo ítems rechazados por falta de stock; acción "Cancelar" (visible solo si `estado=pendiente` y `can('update', $record)`) invoca `CancelarPreventaAction`. `ListVentas::getHeaderActions()` expone `CreateAction` sin chequeo manual — Filament ya la oculta sola si `VentaPolicy::create()` deniega, así el Cajero comparte la misma pantalla sin ver el botón de alta.
      - **`VentaInfolist`**: detalle completo (ticket, estado, cliente/cliente_temporal, vendedor, cajero, pago, líneas con imagen/marca/categoría/atributos igual que `TraspasoInfolist`).
      - **Refactor menor sin cambio de comportamiento:** se extrajo `SelectorProductoVariante::opcionesProductos()` (antes duplicada inline en `TraspasoForm`) para que `VentaForm` no mantenga una segunda copia de la misma consulta — `TraspasoForm` retocado para usar el helper, sin cambios de comportamiento (mismos tests de Fase 3 en verde).
      - **Bug real encontrado y corregido durante este paso (documentado en memoria — ver `filament-enum-select-statecast-bug`):** `Select::options(VentaMetodoPago::class)` activa un `EnumStateCast` de Filament que hace que `Get()` devuelva la **instancia** del enum en cualquier closure reactivo del formulario, mientras que `$data[...]` dentro del `->action()` final ya llega como el **valor string** — comparar `$get('metodo_pago') !== VentaMetodoPago::Efectivo->value` (instancia vs. string) daba `true` siempre, dejando `referencia_pago` marcado como obligatorio sin importar el método de pago, y bloqueaba el cobro **en silencio** (sin excepción, sin log, `assertSuccessful()` seguía pasando). Corregido comparando contra el caso del enum directamente (`!== VentaMetodoPago::Efectivo`) y quitando un `::from()` innecesario sobre `$data['metodo_pago']` (ya es la instancia tipada). Detectado por un `assertHasNoTableActionErrors()` puesto **después** de `callMountedTableAction()` (no solo antes).
    - **Verificado:** 8 tests nuevos (`VentaUiTest`) — Vendedor crea pre-venta desde el formulario, validación cruzada de cliente/cliente_temporal, Cajero recibe 403 en `/pos/ventas/create`, aislamiento por sucursal en `/pos/ventas` con dos Vendedores de sucursales distintas (cumple el recordatorio de la regla 9), visibilidad cruzada de los botones "Cobrar" (solo Cajero)/"Cancelar" (solo Vendedor), cobro real desde la tabla (descuenta stock, libera comprometido, marca `usuario_id`), y que el Vendedor no tiene la habilidad `cobrar`. **Suite completa: 86/86, sin regresiones.**
    - **Mejora de UX post-cierre (sesión 2026-08-05, pedida por el propietario tras usar el formulario con datos reales):** dos problemas de usabilidad detectados en `VentaForm` (panel `pos`, alta de pre-venta): (1) la Sección "Cliente" y la Sección "Productos" quedaban una al lado de la otra (2 columnas por defecto de Filament v5 sin `columnSpanFull()`), dejando la sección corta de Cliente con espacio vacío debajo mientras Productos se extendía en la columna vecina; (2) agregar cada producto requería 3 pasos (elegir Producto → elegir Variante → clic "Agregar otro producto" para la siguiente línea), sin soporte para lector de código de barras pese a que `producto_variantes.codigo_barras` existe justo "para escáner" (`DATABASE.md`).
      - **Layout:** `columnSpanFull()` agregado a ambas Sections — ahora Cliente siempre queda arriba (ancho completo) y Productos abajo (ancho completo).
      - **Buscador único de "Producto + Variante":** nuevo `Select buscador_producto` (transitorio, `dehydrated(false)`) sobre `SelectorProductoVariante::opcionesVariantesConProducto()` (método nuevo) — resuelve la variante exacta en un solo campo buscable por nombre de producto, atributos, código interno **y código de barras**, sin la cascada Producto→Variante de dos pasos (esa cascada se mantiene sin cambios en `TraspasoForm`/Ajuste de Inventario, donde no hay la misma presión de velocidad de caja). Al elegir un resultado, `afterStateUpdated` inyecta una fila en el Repeater `detalles` (cantidad inicial 1) o, si la variante ya estaba en la canasta, incrementa su cantidad en vez de duplicar la fila — y limpia el buscador para el siguiente escaneo/búsqueda. El botón nativo "Agregar otro producto" del Repeater se ocultó (`addable(false)`); cada fila ahora solo expone `cantidad` y el toggle de mayorista editables (el `producto_variante_id` se resuelve vía `Hidden`, ya no hay Selects de Producto/Variante por fila) más un preview enriquecido (`SelectorProductoVariante::renderPreviewVariante()`, nuevo) y el precio calculado — para cambiar de variante, se elimina la fila (acción nativa del Repeater) y se vuelve a buscar.
      - **Bug real encontrado y corregido durante la implementación:** el primer intento de `afterStateUpdated` mutaba una `Illuminate\Support\Collection` con `$coleccion[$indice]['cantidad'] = ...` — Laravel lanza `"Indirect modification of overloaded element ... has no effect"` porque el acceso indexado a una Collection no devuelve una referencia modificable para arrays anidados. Detectado por un test nuevo que ejercita el flujo real vía `Livewire::test()->set('data.buscador_producto', ...)` (no `fillForm()` directo, que hubiera evitado pasar por `afterStateUpdated` y no habría atrapado el bug). Corregido operando sobre un array PHP plano en vez de una Collection.
      - **Tests:** 1 test nuevo (`test_buscador_de_producto_agrega_una_fila_a_la_canasta`) que verifica alta de fila, incremento de cantidad al buscar el mismo producto dos veces, limpieza del buscador tras agregar, y que el cierre del formulario completo sigue creando el `venta_detalle` correcto. Los 2 tests existentes que llenaban `detalles.0.producto_id` manualmente se limpiaron (ese campo ya no existe en el esquema). **Suite completa: 112/112, sin regresiones. Grupo `concurrency`: 2/2.**
      - **Pendiente de verificación manual:** este cambio de UI no se probó en navegador real (sin herramienta de automatización de browser disponible en esta sesión) — antes de darlo por cerrado del todo, confirmar visualmente que el layout apilado y el flujo de búsqueda se sienten bien en la práctica, especialmente con un lector de código de barras físico si el negocio ya tiene uno.
- [x] **Paso 4.4: Configuración de Negocio editable por el admin (tabla `configuracion_negocio`, ver `DATABASE.md` sección 9):** migración de la tabla (fila única), modelo `ConfiguracionNegocio` con accessor cacheado (`ConfiguracionNegocio::actual()`, invalidado al guardar), y una Page dedicada en el panel `admin` (no un Resource) para editar `umbral_mayor`, `soles_por_punto`, `valor_por_punto`. **Incluye migrar** el `PrecioService` de Fase 2 para que lea `umbral_mayor` desde `ConfiguracionNegocio::actual()` en vez de `config('precios.umbral_mayor')` — `config/precios.php` queda obsoleto después de este paso.
    - **Implementado:** migración `2026_08_04_000001_create_configuracion_negocio_table.php` (fila única `id = 1`, insertada en el propio `up()`, con los defaults de `DATABASE.md`: `umbral_mayor=24`, `soles_por_punto=30`, `valor_por_punto=0.30`). Modelo `App\Models\ConfiguracionNegocio::actual()` (accessor cacheado con `Cache::rememberForever()`, invalidado en `booted()::saved()`). `App\Filament\Pages\ConfiguracionNegocioPage` — Page simple (no Resource, fila única no se crea/elimina) solo en panel `admin`, `canAccess()` restringido a `admin`, formulario inline (sin modal, a diferencia de `AjusteInventario`) construido sobreescribiendo `content(Schema $schema)` con `Form`+`EmbeddedSchema` (patrón de `Filament\Pages\Tenancy\EditTenantProfile`, sin necesitar una vista Blade custom). `PrecioService::calcularPrecio()` retocado para leer `ConfiguracionNegocio::actual()->umbral_mayor` en vez de `config('precios.umbral_mayor')`; `config/precios.php` eliminado (sin referencias restantes).
    - **Bug real encontrado y corregido durante este paso:** cachear la instancia completa del modelo Eloquent con `Cache::rememberForever()` rompía en el store real `database` de este proyecto — `config('cache.serializable_classes')` está en `false` (default de seguridad de Laravel 13), lo que hace que `unserialize()` descarte cualquier objeto cacheado devolviendo `__PHP_Incomplete_Class` en vez del modelo (solo arrays/escalares sobreviven). **No detectado por la suite normal** porque `phpunit.xml` usa `CACHE_STORE=array` (no serializa de verdad) — se detectó recién al verificar manualmente contra el Postgres real de desarrollo vía Tinker. Corregido cacheando `->getAttributes()` (array plano) y reconstruyendo el modelo con `newFromBuilder()` en cada llamada a `actual()`. Test de regresión nuevo que fija el contrato (`test_lo_cacheado_es_un_array_plano_no_la_instancia_del_modelo`) para que no se reintroduzca este patrón en Fase 4.5 (`movimientos_puntos`) ni en cualquier otro accessor cacheado futuro.
    - **Verificado:** migración aplicada en Postgres real + verificación manual vía Tinker (`ConfiguracionNegocio::actual()`, invalidación de caché tras `update()`, `PrecioService` reflejando el nuevo umbral sin reiniciar el proceso). 8 tests nuevos (`ConfiguracionNegocioTest`: valores por defecto, caché no refleja cambios externos hasta invalidarse, invalidación al guardar, contrato de array plano cacheado; `ConfiguracionNegocioPageTest`: 403 no-admin, 200 admin, formulario precargado con los valores actuales, guardado persiste y refleja en `actual()`, validación de `umbral_mayor` > 12; `PrecioServiceTest`: usa el umbral configurado, refleja cambios en caliente, excepción si el umbral configurado no supera el umbral de Docena). **Suite completa: 98/98, sin regresiones.**
- [x] **Paso 4.5: Fidelización por puntos (ver `LOGICA_NEGOCIO.md` sección 9, diseño ya cerrado):** migraciones de `clientes.puntos_acumulados` y `movimientos_puntos`, columnas `puntos_ganados`/`puntos_utilizados`/`descuento_por_puntos` en `ventas`, y la lógica en el Action de cierre de venta: calcular puntos ganados, ofrecer canje al Cajero si el cliente está registrado, aplicar `lockForUpdate()` sobre la fila del cliente, y registrar el movimiento en `movimientos_puntos` (mismo principio que el Kardex de inventario — nunca modificar `puntos_acumulados` directamente). Lee `soles_por_punto`/`valor_por_punto` desde `ConfiguracionNegocio::actual()` (Paso 4.4), no desde un archivo de config.
    - **Decisión confirmada con el propietario (sesión 2026-08-04):** el canje de puntos lo ofrece y aplica exclusivamente el **Cajero** al cerrar la venta — el Vendedor puede mencionárselo al cliente en piso, pero el sistema no le da esa acción (arma la canasta y nunca maneja dinero ni finaliza la venta, ver `LOGICA_NEGOCIO.md` sección 2). Evita duplicar el recálculo del total en dos puntos del flujo y una ventana donde se "comprometen" puntos antes del pago real.
    - **Implementado:** 3 migraciones aditivas (`clientes.puntos_acumulados` unsignedInteger default 0; `movimientos_puntos` — Kardex con `created_at` únicamente, mismo patrón que `movimientos_inventario`; `ventas.puntos_ganados`/`puntos_utilizados`/`descuento_por_puntos`). Enum `App\Enums\TipoMovimientoPuntos` (ganado/canjeado/ajuste, mismo patrón que `TipoMovimientoInventario`). `App\Exceptions\PuntosInsuficientesException`. `App\Actions\Puntos\RegistrarMovimientoPuntosAction` — único punto de entrada para modificar `clientes.puntos_acumulados`, calco directo de `RegistrarMovimientoInventarioAction` (`lockForUpdate()` sobre la fila del cliente dentro de `DB::transaction()`, delta con signo según tipo, rechaza si el saldo quedaría negativo). `CerrarVentaAction` retocado con `aplicarFidelizacion()`: solo aplica si `venta->cliente_id` no es nulo; si el Cajero indica `puntos_utilizados`, calcula el descuento (`puntos × valor_por_punto`), lo valida contra el total (rechaza si lo supera) y registra el movimiento `canjeado`; los puntos ganados se calculan sobre el **total ya descontado** (`DATABASE.md` aclara que `descuento_por_puntos` queda "reflejado en total", así que "el total de la venta" de la regla de acumulación es el monto final, no el bruto) y solo generan movimiento `ganado` si son mayores a cero. `VentasTable` (panel `pos`): campo "Puntos a canjear" en el modal "Cobrar", visible solo si `cliente_id !== null`, con `maxValue` acotado al saldo actual del cliente y helper text mostrándolo; notificación de cierre incluye los puntos ganados si aplica. `VentaInfolist` muestra puntos ganados/canjeados/descuento cuando la venta tiene cliente registrado.
    - **Verificado:** flujo completo (canje + ganancia + rollback ante canje que excede el total o el saldo) probado vía Tinker contra Postgres real dentro de una transacción revertida. 18 tests nuevos: `RegistrarMovimientoPuntosActionTest` (ganar, canjear, canje que excede saldo se rechaza sin dejar rastro en el Kardex, ajuste con delta negativo, cantidad cero inválida); 6 tests nuevos en `CerrarVentaActionTest` (gana puntos sobre el total final, `cliente_temporal` no acumula, canje descuenta el total correctamente, canje que supera el total de la venta revierte todo incluyendo el stock ya descontado, canje que excede el saldo del cliente revierte todo, solo un cliente registrado puede canjear); 1 test nuevo en `VentaUiTest` (el Cajero canjea puntos desde el modal "Cobrar" de la tabla y el saldo del cliente refleja el resultado neto). **Nota de testing:** se intentó además un test de UI que verificaba con `assertSee`/`assertDontSee` que el campo "Puntos a canjear" solo aparece para ventas con cliente registrado — se descartó por resultar no determinístico entre dos instancias `Livewire::test()` distintas en el mismo método (comportamiento de caché de schema de Filament ajeno a este cambio, no reproducible de forma aislada); la regla de negocio real que importa (`cliente_temporal` no puede canjear) ya queda cubierta de forma sólida a nivel de Action. **Suite completa: 111/111, sin regresiones.**
    - **Corrección post-cierre (sesión 2026-08-05, revisión de salud del sistema antes de Fase 5):** faltaba el segundo nivel de prueba exigido por la regla 17 de `CLAUDE.md` para `RegistrarMovimientoPuntosAction` — su `lockForUpdate()` real sobre la fila de `clientes` nunca se había probado con paralelismo real de sistema operativo (solo el invariante secuencial en la suite normal). Agregado `tests/Feature/Concurrency/PuntosConcurrenciaTest.php` + `tests/concurrency/ejecutar_canje_puntos.php`, mismo patrón exacto que `InventarioConcurrenciaTest`/`ejecutar_salida_inventario.php` (Fase 3): dos procesos PHP independientes canjean 7 puntos c/u sobre un saldo inicial de 10 — confirma que solo uno tiene éxito, el saldo final queda en 3 (nunca negativo) y solo se crea un registro en `movimientos_puntos`. **Grupo `concurrency`: 2/2. Suite normal sin cambios: 111/111.**

---


## FASE 5: Optimizaciones (Plantillas y Compras)
**Objetivo:** Agilizar procesos recurrentes de la librería.

> ~~**PENDIENTE OBLIGATORIO ANTES DEL PASO 5.2 (Compras) — revisión de salud del Kardex (post Fase 3.7):** agregar un `CHECK (cantidad >= 0)` a nivel de base de datos en la tabla `inventarios`.~~ **RESUELTO (sesión 2026-08-05).** `database/migrations/2026_08_05_000002_add_check_cantidad_no_negativa_to_inventarios_table.php` agrega `CHECK (cantidad >= 0)` vía `DB::statement()` — la garantía de "nunca negativo" ya no depende solo de la disciplina de código en `RegistrarMovimientoInventarioAction`, ahora también la blinda Postgres. **Solo aplica en el driver `pgsql`** (la migración detecta `Schema::getConnection()->getDriverName()` y es no-op en SQLite): SQLite, usado únicamente por la suite de tests en memoria (`CLAUDE.md` regla 17), no soporta agregar un `CHECK` a una tabla existente vía `ALTER TABLE` — mismo motivo por el que `lockForUpdate()` real solo se verifica contra Postgres. Verificado con un insert directo de `cantidad = -5` vía Tinker contra Postgres real, rechazado por el constraint (`QueryException` con el nombre `inventarios_cantidad_no_negativa`). **Suite completa: 116/116. Grupo `concurrency`: 2/2. Sin regresiones.**
> Notas menores del mismo análisis, sin acción requerida por ahora: (a) deadlock teórico si dos traspasos concurrentes entre los mismos dos almacenes corren en direcciones opuestas — Postgres lo aborta de forma segura (no corrompe datos), solo falta un mensaje amigable/reintento si algún día se vuelve un problema real; (b) posible comando de reconciliación periódica (`inventarios.cantidad` vs. suma de `movimientos_inventario`) como herramienta de auditoría, no de corrección de bugs — opcional, sin fecha.

- [x] **Paso 5.1: Plantillas Escolares:** Desarrollo de `listas_escolares`. Lógica para que un vendedor seleccione "2do Básico - San Calixto" y el sistema agregue 15 productos a la canasta de golpe.
    - **Decisiones confirmadas con el propietario (sesión 2026-08-05):** (1) solo `admin` crea/edita plantillas, mismo criterio que el resto del catálogo maestro; (2) `es_plantilla` se agrega como columna (fuente de verdad `DATABASE.md`) pero **sin lógica de borrador/publicada** en este MVP — nadie lo pidió como regla de negocio real, se reabre si aparece un caso concreto; (3) `precio_total_estimado` se **autocalcula y cachea**, nunca se escribe a mano.
    - **Implementado:** migraciones `listas_escolares` (softDeletes) y `lista_escolar_detalles` (cascadeOnDelete al padre, restrictOnDelete a `producto_variantes`, mismo patrón que `traspaso_detalles`). Modelos `ListaEscolar`/`ListaEscolarDetalle` (`productoVariante()` con `withTrashed()` desde el inicio, aplicando la nota del Paso 3.11). `ListaEscolarPolicy` (10 métodos, admin-only — mismo patrón estricto que `SucursalPolicy`). `App\Filament\Resources\ListasEscolares\ListaEscolarResource` (panel `admin`, con `DetallesRelationManager` reutilizando la cascada Producto→Variante de `SelectorProductoVariante`, campo `precio_total_estimado` deshabilitado/no-dehidratado en el form — es puramente informativo). `App\Observers\ListaEscolarDetalleObserver` (`created`/`updated`/`deleted`) recalcula `precio_total_estimado` sumando `precio_venta_unidad × cantidad` de cada línea — efecto secundario simple de un solo registro directamente dependiente (excepción de `CLAUDE.md` regla 2), registrado en `AppServiceProvider`.
    - **En `VentaForm` (panel `pos`):** nuevo `Select aplicar_lista_escolar` (junto al `buscador_producto` ya existente, ambos en una Section "Productos" ahora de 2 columnas) que agrega TODAS las líneas activas de la plantilla a la canasta de un golpe, reutilizando el mismo helper `agregarOIncrementarDetalle()` que ya usaba `buscador_producto` (extraído a un método compartido junto con `detallesLimpios()` para no duplicar la lógica de merge). Líneas de productos descontinuados/en papelera (`estado=false` o `trashed()`) se omiten con una notificación indicando cuántas se saltaron — nunca rompe el alta de la venta.
    - **Bug real encontrado y corregido durante la implementación:** el modelo `ListaEscolar` no declaraba `protected $table`, así que Eloquent adivinaba la tabla como `lista_escolars` (pluralización en inglés del último segmento) en vez de `listas_escolares` (la tabla real) — reventaba con `SQLSTATE... no such table: lista_escolars` en cualquier pantalla que tocara el modelo. `ListaEscolarDetalle` no necesitó el mismo fix porque su pluralización naive sí coincide por casualidad con `lista_escolar_detalles`. Detectado de inmediato al correr los tests de `VentaUiTest` (no en producción), gracias a que la suite se corre después de cada cambio.
    - **Tests:** `ListaEscolarResourceTest` (403 no-admin, 200 admin, recálculo automático de `precio_total_estimado` al crear/borrar detalles vía el Observer). 2 tests nuevos en `VentaUiTest` (mismo rigor que el buscador de productos — `Livewire::test()->set()` sobre el flujo real, no `fillForm()`): aplicar una plantilla agrega todas sus líneas y sumar cantidades si se reaplica sin duplicar filas; una línea de producto descontinuado se omite sin romper el resto. **Suite completa: 121/121. Grupo `concurrency`: 2/2. Sin regresiones.**
- [x] **Paso 5.2: Compras a Proveedores:** Ingreso de mercadería nueva (`compras`, `proveedores`). Al confirmar una compra, el sistema inyecta stock a través de `InventoryAction`.
    - **Decisiones confirmadas con el propietario (sesión 2026-08-05), tras aclarar la diferencia entre Ajuste Manual (admin, corrección administrativa sin origen comercial) y Compras (reabastecimiento real con proveedor):**
      1. **Flujo de un solo paso**, no dos etapas: el que registra la compra anota lo que físicamente llegó (proveedor, almacén, líneas con cantidad y precio) y el stock entra de inmediato como `completado` — no existe un "pedido" previo dentro del sistema. Coincide exactamente con `DATABASE.md` (`compras.estado` solo `completado`/`anulado`, sin `cantidad_recibida` en `compra_detalles`). El flujo de dos etapas (pedido → recepción parcial, que `CLAUDE.md` regla 18 había anticipado) queda anotado como **mejora futura**, se reabre si el negocio lo pide.
      2. **Tanto `admin` como Almacenero pueden registrar compras** — no es exclusivo del Almacenero pese a que `LOGICA_NEGOCIO.md` lo describe como quien "gestiona" el ingreso (no dice "exclusivamente"; es común que el propietario trate directo con proveedores). El admin elige libremente el almacén de destino (sin restricción, mismo principio de acceso global de `CLAUDE.md` regla 9); el Almacenero queda restringido a los almacenes que tiene asignados vía `almacen_usuario` (`AislaPorAlmacen`, mismo criterio "hub and spoke" que `InventarioResource`/`TraspasoResource`) — si tiene uno solo, se precarga y el campo queda deshabilitado; si tiene varios, elige entre los suyos.
      3. **Reutiliza el patrón "por caja"** del Ajuste Manual (Paso 3.4, mejora sesión 2026-08-05) sin reinventarlo.
    - **Implementado:**
      1. Migraciones `proveedores`, `compras`, `compra_detalles` — exactamente como en `DATABASE.md`, sin cambios. Modelos `Proveedor`/`Compra`/`CompraDetalle` (`Compra::proveedor()`/`CompraDetalle::productoVariante()` con `withTrashed()` desde el inicio, Paso 3.11). **Bug real encontrado y corregido de inmediato (antes de que llegara a romper nada):** `Proveedor` sin `$table` explícito hacía que Eloquent adivinara `proveedors` en vez de `proveedores` — mismo tipo de bug que `ListaEscolar` en el Paso 5.1, esta vez detectado proactivamente vía `Str::plural()` en Tinker antes de escribir el modelo completo, no como un fallo de test posterior.
      2. **`App\Filament\Support\CamposCantidadPorCaja`** — el bloque "Por unidad / Por caja" de `AccionAjusteInventario` se extrajo a un componente compartido (primera extracción real, justo al llegar al segundo uso, tal como quedó anotado la sesión pasada): `campos(bool $permitirNegativo)` (Ajuste admite +/-, Compras no — una compra siempre suma), `sincronizarUnidadesPorCaja()`, `cantidadFinalEnUnidades()`, `actualizarTamanoCajaSiCambio()`. `AccionAjusteInventario` retocado para usarlo sin cambiar su comportamiento (suite en verde, sin regresiones).
      3. **`RegistrarCompraAction`**: crea `Compra` + `CompraDetalle` por línea, resuelve cada línea "por caja" (incluyendo la actualización del tamaño de caja del producto) y llama a `RegistrarMovimientoInventarioAction` (`tipo_movimiento = ingreso`) — todo en una sola `DB::transaction()`, mismo patrón que `CrearPreventaAction`. **`AnularCompraAction`**: revierte stock línea por línea (`salida`) y marca `anulado`; si ya no hay stock suficiente para revertir (porque se vendió/traspasó desde entonces), `StockInsuficienteException` se propaga y la transacción revierte todo — la compra nunca queda anulada a medias.
      4. **`ProveedorPolicy`** (admin-only, 10 métodos) y **`CompraPolicy`** (10 métodos: `viewAny`/`view`/`create` para admin y almacenero; `update`/`delete*`/`restore*` en `false` — documento de auditoría, igual que Venta/Traspaso; habilidad custom `anular()` restringida a `admin` únicamente, mismo criterio que Ajuste Manual).
      5. **`ProveedorResource`** solo en panel `admin` (catálogo maestro, con papelera). **Dos `CompraResource`** (uno por panel, comparten modelo/Action/Policy, `CLAUDE.md` regla de multi-panel) que a su vez comparten los builders `App\Filament\Support\CompraForm`/`CompraTable`/`CompraInfolist` (parametrizados por `?Collection $almacenesPermitidos`, mismo espíritu que `AccionAjusteInventario::make(?Almacen $almacenFijo)`) — evita duplicar el formulario/tabla/infolist casi idénticos entre paneles. `CreateCompra` (panel `almacen`) nunca confía a ciegas en el `almacen_id` del formulario: si el Almacenero tiene un solo almacén, se usa ese directo (ignora el campo, que además ya viene deshabilitado); si tiene varios, valida server-side que el elegido esté entre los suyos (`abort_unless`) — **dos capas de defensa**, la primera ya la da gratis el propio `Select` de Filament (solo ofrece almacenes propios como opciones válidas, así que un valor ajeno ya falla la validación del formulario antes de llegar al `abort_unless`), confirmado con test dedicado. Proveedor con `createOptionForm` en el formulario de compra (mismo patrón que `Cliente` en `VentaForm`, Fase 4.3).
      6. **Sin página de edición:** las compras no se editan (Policy `update() = false`, documento histórico) — solo `index`/`create`/`view`, mismo patrón que `VentaResource`.
    - **Verificado:** flujo completo (compra por unidad, por caja, con varias líneas, anulación con reversión, anulación rechazada por stock insuficiente) probado primero contra Postgres real vía Tinker dentro de una transacción revertida, luego codificado como tests permanentes. 7 tests en `RegistrarCompraActionTest` (Action pura, sin Livewire) + 9 en `CompraResourceTest` (acceso por rol/panel, creación admin sin restricción, precarga/restricción de almacén del Almacenero incluyendo el caso de almacén ajeno, **precarga reactiva de `unidades_por_caja` dentro de una línea del Repeater probada con `Livewire::test()->set()` sobre el flujo real** — mismo rigor que el buscador de productos de Fase 4.3, no `fillForm()` directo — y anulación desde la tabla revirtiendo stock) + 2 en `ProveedorResourceTest`. **Suite completa: 139/139. Grupo `concurrency`: 2/2. Sin regresiones.**
- [x] **Paso 5.3: Adelantos de Sueldo:** Un módulo simple (CRUD) para registrar dinero entregado a empleados antes de fin de mes.
    - **Decisiones confirmadas con el propietario (sesión 2026-08-07):** (1) acceso exclusivo `admin`, panel `admin` únicamente — información financiera sensible de nómina, mismo criterio que Sucursal/Almacen/User; `registrado_por_id` siempre es el admin autenticado, nunca un valor del formulario; (2) sin módulo de planilla en el sistema, la transición `pendiente_descuento` → `descontado` es un toggle manual (acción "Marcar como descontado" en la tabla) que el admin dispara cuando procesa el descuento en su planilla externa — el sistema solo registra el estado, no calcula ni descuenta nada automáticamente; (3) `usuario_id` (empleado que recibe) se restringe a `users.is_active = true`, sin filtrar por rol (un admin también podría recibir un adelanto, dueño único del negocio).
    - **Implementado:** migración `adelantos_sueldo` exactamente como en `DATABASE.md` sección 7 (sin `SoftDeletes`, no es un documento con papelera — a diferencia de Venta/Traspaso/Compra, si el admin comete un error de tipeo simplemente edita o borra el registro). Enum `App\Enums\AdelantoSueldoEstado` (`HasColor`/`HasLabel`, mismo patrón que `CompraEstado`). Modelo `App\Models\AdelantoSueldo` (`usuario()`/`registradoPor()`, ambas `belongsTo(User::class)`; **bug real encontrado y corregido de inmediato** — mismo tipo exacto que `ListaEscolar`/`Proveedor`: sin `$table` explícito, Eloquent adivinaba `adelanto_sueldos` en vez de `adelantos_sueldo`, detectado al correr los tests nuevos antes de darlo por cerrado, no en producción). `App\Policies\AdelantoSueldoPolicy` (10 métodos, admin-only). `App\Filament\Resources\AdelantosSueldo\AdelantoSueldoResource` (panel `admin` únicamente, sin relación con `pos`/`almacen`): `Select usuario_id` filtrado a activos con label `"{nombres} {apellidos} ({rol})"`; campo `estado` visible solo en edición, deshabilitado y no-dehidratado (informativo — el único camino real para cambiarlo es la acción de tabla, evitando dos rutas distintas para el mismo cambio, mismo criterio que las transiciones de estado de `Traspaso`); `CreateAdelantoSueldo::mutateFormDataBeforeCreate()` fija `registrado_por_id`/`estado` desde el servidor (nunca del formulario, mismo criterio que `CreateVenta`/`CreateCompra`); acción de tabla "Marcar como descontado" (visible solo si `estado = pendiente_descuento`) + `EditAction`/`DeleteAction` estándar (sin papelera) + `SelectFilter` por estado.
    - **Verificado:** flujo completo (alta, relaciones, transición de estado) probado vía Tinker contra Postgres real dentro de una transacción revertida. 5 tests nuevos (`AdelantoSueldoResourceTest`): 403 no-admin, 200 admin, alta con `registrado_por_id`/`estado` correctos, un usuario inactivo no es una opción válida del Select (mismo criterio de validación que `almacen_id` en `CompraForm` — Filament rechaza a nivel de formulario, no hace falta lógica adicional), la acción "Marcar como descontado" cambia el estado y deja de estar visible una vez aplicada. **Suite completa: 144/144. Grupo `concurrency`: 2/2. Sin regresiones.**

**FASE 5 — COMPLETA (5.1 a 5.3).**

---

## FASE 6: Dashboard de Estadísticas (Panel Admin — Propietario)
**Objetivo:** Darle al propietario una vista clara del negocio sin que tenga que interpretar tablas técnicas. No requiere cambios al esquema de base de datos; se construye enteramente sobre `ventas` y `venta_detalles`.

- [ ] **Paso 6.1: `ReporteService`:** Centralizar en `app/Services/ReporteService.php` las consultas de agregación necesarias (ventas por período, producto más vendido por cantidad y por monto, vendedor con más ventas/clientes atendidos, ventas por sucursal, por método de pago, por origen, categorías/marcas más vendidas, alertas de stock bajo según `stock_minimo` — `cantidad <= stock_minimo`). Todas deben aceptar un rango de fechas y, opcionalmente, filtro por sucursal.
- [ ] **Paso 6.2: Widgets en Filament (panel `admin`):**
    - Widgets tipo "Stats Overview" para las tarjetas rápidas (total vendido, número de ventas, ticket promedio, comparación vs. período anterior).
    - Widgets tipo "Chart" para los rankings y distribuciones (producto más vendido, vendedor top, ventas por sucursal, por método de pago).
    - Filtro global de rango de fechas en el dashboard (hoy/semana/mes/personalizado).
- [ ] **Paso 6.3: Alcance por panel:** Estas estadísticas globales viven únicamente en el panel `admin`. Los paneles `pos` y `almacen` no las muestran, salvo que más adelante se decida dar a Vendedor/Almacenero indicadores acotados a su propia sucursal o almacén.

---

## FASE 7: API y E-Commerce (El Frontend Externo)
**Objetivo:** Conectar la tienda web de React al núcleo de Laravel.

> **Reordenada al final deliberadamente (antes Fase 6):** la prioridad es que el sistema funcione por completo para los roles internos de la tienda (`admin`, Vendedor, Cajero, Almacenero) antes de abrir un canal externo. Esta fase queda como un placeholder de 3 pasos por ahora; cuando llegue el momento de retomarla se desglosará en sub-fases más detalladas (autenticación, catálogo público, carrito, checkout, etc.), igual que se hizo con Fase 1.5 o Fase 3.

- [ ] **Paso 7.1: API Endpoints:** Crear rutas de API protegidas para el catálogo de productos.
- [ ] **Paso 7.2: Autenticación Web:** Permitir a los clientes iniciar sesión usando **Laravel Sanctum** (decidido — más simple que JWT y nativo de Laravel; adecuado para una SPA en React que consume la misma API, sin necesitar la complejidad de tokens firmados de JWT).
- [ ] **Paso 7.3: Lógica de Carrito Web:** Cuando la app de React envía una orden confirmada, Laravel reutiliza EXACTAMENTE la misma lógica de creación de venta y descuento de stock (Fase 4 y Fase 3), pero marcando el `origen` como `ecommerce`.
